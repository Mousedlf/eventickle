{% extends 'base.html.twig' %}

{% block title %}ComedyClub{% endblock %}

{% block body %}
    <h1>ComedyClub</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comedy_club.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ comedy_club.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comedy_club_index') }}">back to list</a>

    <a href="{{ path('app_comedy_club_edit', {'id': comedy_club.id}) }}">edit</a>

    {{ include('comedy_club/_delete_form.html.twig') }}
{% endblock %}
